meta {
  name: users exist
  type: http
  seq: 12
}

get {
  url: {{api}}/admin-tools/migrate-permissions
  body: none
  auth: bearer
}

headers {
  x-api-key: {{apiKey}}
}

auth:bearer {
  token: {{token}}
}
